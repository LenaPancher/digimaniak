import {View, Text} from 'react-native';
import React, {useCallback} from 'react';

const digimonCard = () => {
  const digimonImage = useState('');

  const getDigimonImage = useCallback(() => {});

  return (
    <View>
      <Text>digimonCard</Text>
    </View>
  );
};

export default digimonCard;
